# ErtHub — зам, тээврийн нээлттэй өгөгдлийн портал

Статик сайт (build алхамгүй). `index.html` дотор `<script type="text/x-dc">`
блокт бүх логик; темплейт нь `dc-runtime.js` (`sc-for` / `sc-if` / `{{ }}`).

## Файлын үүрэг

| Файл | Юу хадгалдаг |
|---|---|
| `index.html` | Сайт бүхэлдээ (темплейт + логик, ~7500 мөр) |
| `admin/index.html` | Удирдлагын хэрэгсэл — текст ба метрик холбоос засах |
| `content.json` | **Бүх харагдах текст** (доор үз) |
| `metric_registry.json` | Виджет → метрикийн холбоос, `agg/filter/transform` |
| `js/erthub-publish.js` | Backend overlay — админаас сайтад ШУУД нийтлэх (`/api/site-content`, Postgres) |
| `firestore.rules` | Хэрэглэгчийн датаны (профайл, сэтгэгдэл, тоолуур) эрхийн дүрэм — нийтлэл ЭНД БИШ |
| `tests/run.js` | Ерөнхий регресс (F = засварлагдалт, G/G3 = нийтлэл, Z = анхны сэтгэгдэл; `--only=Z`, `--only=G3`) |
| `.claude/skills/first-impression-audit/` | `ux-qa-persona` агентын скилл — дэлгэц бүрийг анх орж буй хүний нүдээр 8 асуултаар шалгах |
| `tests/text-coverage.js` | Харагдах текстийн хамрах хүрээ + round-trip (5 тест) |

## content.json — 4 блок

- `widgets{}` — виджет тус бүрийн `title` / `sub` / `unit` / `foot` / `foot2`
- `site{}` — нүүр хуудасны виджет БУС бүх текст (толгой, hero, KPI, шүүлт,
  7 хоногийн тайлан, коммунити, AI, эх сурвалжийн мета г.м.)
- `ui{}` — **админы** интерфейсийн текст (badge, товч, шошго). Нэг удаа
  бүртгэгдэж виджет бүрийн засварын дэлгэцэд давтагдана
- `ui_form{}` — админы маягтын бүлэг/шошго өөрөө

Уншилт: `index.html` → `stxt(path, fallback)` (модулийн түвшинд, класс
гаднаас ч уншигдана) ба `txt(id, field, fallback)`.
`applySiteContent()` нь `SECTORS` / `KPI_UNIFIED` / `WEEK_DATA` / `RECENT` /
`PROJECTS` / `POLICIES` / `AI_*` тогтмолыг content.json-оос дүүргэнэ.
`admin/index.html` → `utxt(path, fallback)` ба `data-ui` атрибут.

Админ ФАЙЛ РУУ ШУУД БИЧДЭГГҮЙ — `Экспортлох` товч JSON татаж өгнө,
түүнийг репод хуулж commit хийнэ.

## Нийтлэх давхарга (backend overlay · Postgres)

Админы `Сайтад нийтлэх` товч нь `CON`/`REG`-ийг etransport-backend-ийн
`PUT /api/site-content` руу бичнэ (`public.site_content_versions`, нийтлэл
бүр шинэ мөр). Сайт нь `loadPublished()`-ээр `GET /api/site-content`-ийг
уншиж **файлын дээр давхарлана**. Firestore ашиглахгүй (сан үүсээгүй).

- **Дараалал ЧУХАЛ**: файл эхлээд ачаалагдаж хуудас зурагдана, overlay
  хожуу ирээд дахин зурна. Backend унтарсан/сүлжээгүй бол сайт ЯГ
  өмнөх шигээ ажиллана — нийтлэл бол НЭМЭЛТ давхарга, шаардлага биш.
- **Давхарлах = гүн нийлүүлэх** (`EHPublish.layer`, сайт ба админ ХОЁУЛАА):
  объект түлхүүрээр нийлнэ, массив/утга нь нийтлэлийнхээр солигдоно. Нийтлэл
  бол снапшот — бүтнээр сольвол дараа нь git-ээр нэмсэн текст алга болно.
  Файл ба нийтлэл аль нь түрүүлж ирсэн ч `applyContentLayers()` /
  `applyRegistryLayers()` ижил үр дүн өгнө (G3.11–G3.14).
- `js/backend-config.js` нь `erthub-publish.js`-ээс **ӨМНӨ** ачаалагдана —
  эс тэгвэл `EHPublish.enabled=false` болж нийтлэл чимээгүй унтарна (G тест).
- Админ ачаалахдаа overlay-г **суурь** болгоно (`CON0=clone(CON)`) —
  эс тэгвэл файлын хуучин текстийг засаж, дараагийн нийтлэлээр амьд
  сайтын өөрчлөлтийг буцааж дарна. Модуль ачаалсан `version`-оо
  `baseVersion` болгон илгээдэг — хооронд нь өөр хүн нийтэлсэн бол 409.
- Postgres-д **`json`** (jsonb БИШ) — jsonb түлхүүрийн дарааллыг өөрчилнө.
- Эрх: Firebase ID token (backend өөрөө шалгана) + `public.portal_admins`
  (uid эсвэл баталгаажсан и-мэйл). Шинэ админ = ганц INSERT, код өөрчлөхгүй.
- Нийтлэл нь шуурхай харагдац, **git нь бүртгэл** — экспорт → commit
  хэвээр хэрэгтэй.
- Backend код: `C:\Users\User\etransport-backend-work` (`src/routes/siteContent.js`,
  `src/auth/firebaseToken.js`, `migrations/009_site_content.sql`).

Тохируулгын алхмууд `README.md` → "Админаас сайтад ШУУД нийтлэх".

## Командууд

```bash
node scripts/serve.js 8080          # хөгжүүлэлтийн сервер
node tests/run.js                   # систем тест (A–I бүлэг)
node tests/text-coverage.js --list  # хамрах хүрээ 100% байх ёстой
```

Commit хийхийн ӨМНӨ заавал:

```bash
node scripts/check-cyrillic.js && node scripts/check-registry.js
```

## Тестийн мөчлөг — ЗААВАЛ баримтлах

Жижиг алдаанууд (preview шинэчлэгдэхгүй, шошго хуучнаараа үлдэх,
англи тэмдэглэгээ ил гарах) нэг нэгээрээ илэрч цаг их иддэг. Иймд
өөрчлөлт бүрийг ДООРХ МӨЧЛӨГӨӨР дуусгана — алхам алгасахгүй:

1. **Unit тест ЭХЛЭЭД.** Логик нэмэх бүрд `tests/run.js`-д тухайн
   зан төлөвийг ГАРААР БОДОХ БОЛОМЖТОЙ жижиг фикстур дээр шалгах тест
   бич (жиш. I1/I4 — 3 мөр дээр SUM/COUNT/AVG/MIN/MAX). Бодит feed
   дээр "зөв харагдаж байна" гэж НҮДЭЭР батлахгүй.
2. **Регресс тест — алдаа бүрд.** Хэрэглэгчийн илрүүлсэн алдаа бүрд
   тэр ЯГ гүйлгээг давтдаг тест нэмнэ (жиш. I5 — админ дээр сонголт
   сольсон даруйд preview-ийн тоо ӨӨРЧЛӨГДӨХ). Ингэснээр нэг алдаа
   хоёр дахин гарахгүй.
3. **ХОЁР талыг ХАМТ шалга.** Тохиргоо нэмбэл (а) админы preview,
   (б) сайт дээрх бодит харагдац ХОЁУЛАНГ нь шалгана. Preview зөв
   мөртөө сайт буруу (эсвэл эсрэгээр) байх нь ЭНЭ ТӨСӨЛД хамгийн
   олон давтагдсан алдаа.
4. **ЦУВРАЛААР нь шалга.** Нэг виджет ажиллав гэдэг нь бусад нь
   ажиллана гэсэн үг БИШ. 16 виджетийг гүйлгэн шалгах (F бүлгийн
   загвар) эсвэл browser-оос давтан шалгана.
5. **Дараа нь л commit.** `check-cyrillic` → `check-registry` →
   `tests/run.js` → `text-coverage.js` дөрвүүлээ ногоон болсны дараа.

**Хамт өөрчлөгддөг зүйлсийн ЖАГСААЛТ** (утга солиход дагаж
өөрчлөгдөх ёстой — алдаа ихэвчлэн эндээс гарна): тоо · **нэгжийн
шошго** · **метрикийн нэр** · **өөрчлөлтийн хувь** · **spark
line/график** · **гарчиг**. Аль нэг нь хуучнаараа үлдвэл хуудас
ХУДАЛ тайлбарлана.

## Админы preview — засварлагдалтын гэрээ

Виджетийн засварын дэлгэц дээр ХАРАГДАЖ БАЙГАА текст бүр засварлагдана.
Гурван эх сурвалж, гурван төрлийн талбар:

| Эх сурвалж | Атрибут | Юу |
|---|---|---|
| `content.json → widgets{}` | `data-tf` | Виджетийн өөрийн текст (title/sub/unit/foot/foot2) |
| `content.json → ui{}` ба `site{}` | `data-sf` | "Preview-ийн нийтлэг текст" — badge, тэнхлэг, салбарын нэр |
| `metric_registry.json` слот | `data-rf` | Холбогдоогүй слотын `label` / `would_need` |

Дүрмүүд:

- **Нэгжийн шошгод content.json ДАВУУ эрхтэй.** Админы `previewUnit()` ба
  сайтын `buildWeek()` хоёул ижил дараалал баримтална:
  `widgets.<id>.unit` → FIXED_UNIT/registry. Preview-д зөвхөн
  `requiredUnit()` уншивал "Нэгжийн шошго" талбар үхмэл болно.
- **График сонгосон МЕТРИКЭЭС хамаарна.** `metricAnySeries(mk)` ашиглана.
  Метрикийн нэрийг хатуу шалгаж (`headMetric(id)==='...'`) дата сонгож
  БОЛОХГҮЙ — өөр метрик сонгоход график өөрчлөгдөхгүй болно.
- **Үхмэл талбар нэмэхгүй.** `chromeFields()` нь зам бүрийг
  `pathAffectsPreview()`-ээр (утгыг нь түр зондоор солиод preview-г дахин
  зурж) шалгана. Preview-д нөлөөлдөггүй зам жагсаалтад ОРОХГҮЙ.
- **Бичих даруйд preview шинэчлэгдэнэ.** `repaintPreview()` нь `#pvhost`-ыг
  л дахин зурна (бүтэн `render()` хийвэл курсор алдагдана).
- **`applyContentText()` ДАХИН дуудагддаг.** Тогтмолуудыг (`SECN`, `SHAPEN`,
  `FL`, `FIXED_UNIT` …) `DEF` хуулбараас дүүргэнэ — эс тэгвэл өмнөх утга
  дараагийн удаагийн fallback болж "наалдаж", буцаах боломжгүй болно.

Эдгээрийг `tests/run.js` F бүлэг 16 виджет × 300+ талбараар шалгана.

## Ажлын горим — шаардлага хэрэгжүүлэх

Хэрэглэгчийн бичсэн шаардлагыг дараах дарааллаар хэрэгжүүл:

1. **Архитектурын хувьд нэгтгэж дүгнэ.** Тусад нь бичигдсэн шаардлагуудыг
   нэг ижил схем болгож хар — content.json-ы блокуудтай, metric_registry.json-той,
   одоо байгаа темплейт бүтэцтэй хэрхэн нийцэхийг эхэлж тодорхойл, дараа нь код бич.
2. **Дэлхийн шилдэг практиктай харьцуулж, судалгаанд үндэслэн шийд.**
   Ижил төрлийн dashboard/data-portal-уудын UX хэвшлийг (навигаци, шүүлт,
   KPI-ийн зохион байгуулалт, эх сурвалжийн ил тод байдал гэх мэт) жишиг
   болгож ав — таамгаар биш.
3. **UI/UX-ийг Browser preview-ээр турших явцдаа сайжруулах боломж олдвол
   шууд өөрөө сайжруул, тусад нь зөвшөөрөл бүү асуу.** Энэ нь хамрагдаж буй
   ажлын цар хүрээнд багтсан, урвуулж болох (fallback-тай, файл унагаахгүй,
   тестийг эвдэхгүй) сайжруулалтад хамаарна.
4. **Зайлшгүй шаардлагатай зөрчил дээр л зогсож асуу.** Өөрөөр хэлбэл:
   шаардлагууд бодитоор зөрчилдөж, хэрэглэгчийн санааг мэдэхгүйгээр аль нэгийг
   сонгож болохгүй тохиолдол, эсвэл өгөгдөл/архитектур эргэлт буцалтгүй
   өөрчлөгдөх тохиолдол. Бусад бүх үед өөрөө шийдвэр гаргаж, таскыг эцсээр нь
   гүйцэтгэ — дутуу орхиж, "ингэж хийе үү?" гэж олонтаа бүү асуу.

(Энэ горим код/дизайны ШИЙДВЭРт хамаарна. Аюулгүй байдлын зөвшөөрлийн
дүрмийг — push хийхийн өмнө зөвшөөрөл авах гэх мэт — орлохгүй, доорх
"Дүрэм" хэсэгт заасан зөвшөөрлийн шаардлага хэвээр хүчинтэй.)

## Дүрэм

- **Тоо ЗОХИОХГҮЙ.** Эх сурвалж холбогдоогүй бол `0` эсвэл `—` + "мэдээлэл
  алга" гэж ил хэлнэ. Зохиомол утга бол алдаа гэж үзнэ.
- **Ганц эх сурвалж.** Текстийг код ба content.json хоёуланд бүү давхардуул.
  Динамикаар тооцоологддог утгыг (`weekPaxUnit` гэх мэт) content.json-д
  бүү бич.
- **Шинэ харагдах текст нэмбэл** content.json-д бүртгэ — эс тэгвэл
  `tests/text-coverage.js` F1/F2 унана (хамрах хүрээ 100%-аас буурна).
- Push хийхийн өмнө зөвшөөрөл ав.
- Тайлбар, commit мессеж монголоор.
