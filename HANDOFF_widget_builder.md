# Шилжүүлгийн хураангуй — etransport_v2 backend → ErtHub widget builder

## Зорилго (энэ chat-д хийх ажил)

`erthub-dataagent` репо (Мал/ЭртХаб — зам, тээврийн нээлттэй өгөгдлийн портал,
`admin/index.html` + `index.html`, статик сайт) нь одоо шинээр гаргасан backend
API-аас **бодит салбарын дата** авч widget builder-т холбогдох ёстой.

## Өмнөх chat-д ХИЙГДСЭН ажил (дэвшилтэт төлөв)

### 1. Сервер, өгөгдлийн сан
- Сервер: `platformbackup@10.2.115.40` (Ubuntu 26.04), дотоод сүлжээ (`10.0.0.0/8`) —
  зөвхөн **Fortinet SSL VPN (MFA)**-аар л хүрдэг. Одоогийн OpenVPN холболт (172.169.x.x/
  192.168.x.x) энэ мужид ХҮРДЭГГҮЙ.
- `etransport_v2` PostgreSQL сан бэлэн: `bronze`/`silver`/`gold` (Medallion) schema.
- Хэрэглэгч: `etransport_app` (нууц үг Password Manager-т).

### 2. Backend API (Node.js/Express)
- Байршил: `/home/platformabackup/etransport-backend` (АНХААР: home directory
  бодитоор **`platformabackup`** гэж алдаатай үүссэн, `platformbackup` БИШ!).
- systemd service: `etransport-backend` (`sudo systemctl status etransport-backend`),
  автоматаар асдаг, `127.0.0.1:3000` дээр ажиллана.
- Endpoint-үүд:
  - `GET /health` → `{"status":"ok",...}`
  - `GET /health/db` → DB холболт шалгах
  - `GET /api/sectors` → 5 салбарын жагсаалт (`hasLiveData` flag-тай)
  - `GET /api/sectors/:sector/summary` → `gold.monthly_summary_by_sector`-с сарын нэгтгэл
    (**АНХААР**: rail-ийн хувьд одоогоор `silver.rail_operations`-г уншдаг, бид
    `silver.rail_wagon_loading`-д (шинэ) дата хийсэн ч ЭНЭ endpoint түүнийг ХАРАХГҮЙ —
    доорх "Дараагийн ажлын жагсаалт"-ыг үз)
  - `GET /api/sectors/:sector/datasets` → `dataset_registry`-с

### 3. Firewall/нээлттэй байдал — ⚠️ ХИЙГДЭЭГҮЙ
- Backend одоогоор ЗӨВХӨН `127.0.0.1` дээр (сервер дотроо) ажиллаж байгааг шалгасан.
- **ErtHub-ийн widget builder GitHub Pages дээр (нийтийн интернэт) байрлаж байгаа тул,
  10.2.115.40:3000 руу шууд хүрч ЧАДАХГҮЙ** — VPN-гүй, дотоод хаяг.
- Widget builder-тай холбохын ӨМНӨ эсвэл хамт: nginx reverse proxy + SSL, эсвэл
  өөр нийтийн endpoint зохион байгуулах шаардлагатай (runbook-ийн 9-р зүйл үз,
  `docs/deploy-runbook.md` серверт байгаа файлаас).

### 4. Дата (ETL) — амжилттай засварлаж, туршиж баталгаажуулав
- **Air**: `npm run etl:air:all` — 50 бодит мөр `bronze→silver→gold` бүрэн урссан.
  TLS алдаа (Sectigo intermediate cert дутуу) болон эрхийн алдаа (public/bronze/
  silver/gold GRANT дутуу) хоёуланг нь олж засав.
- **Rail wagon-loading**: `src/etl/rail/wagonLoadingTransform.js`-ийн field mapping
  **БУРУУ** байсан (код "C34-C64" гэсэн БАЙХГҮЙ дугаарлалт ашигладаг байсан, бодит
  API нь `ID_ST`/`ST_NAME`/`JOB_DT`/`POG_*`/`WIG_*` нэртэй). Хэрэглэгчийн ирүүлсэн
  "Ачилт буулгалтын хоногийн мэдээ _УБТЗ_.xlsx" dictionary-тэй тулгаж, ЗАСАВ.
  Одоо 50/50 мөр амжилттай `silver.rail_wagon_loading`-д орсон, утга шалгаж
  баталгаажуулсан (жиш. Шивээ-Овоо=322, Амгалан=29/89).
- `road`/`sea`/`transit` — эх сурвалж/ETL хараахан БАЙХГҮЙ (`hasLiveData: false`).

### 5. Гол сургамж/анхаарах зүйл дараагийн ажилд
- **Терминал paste-ийн асуудал**: серверийн terminal олон мөрийг зэрэг paste хийхэд
  зарим үед гүйцэтгэхгүй зүгээр текст болгож үлддэг байсан — тул ГАНЦ КОМАНД тус
  бүрчлэн өгч, хариу хүлээж байхыг зөвлөж байна. Харин heredoc (`cat > file <<
  'EOF' ... EOF`) блок ажиллагаатай.
- **Эрхийн алдаа хэвшил**: PostgreSQL 15+-аас хойш шинэ schema/materialized view
  үүсгэхэд `etransport_app`-д GRANT ХИЙГДЭЭГҮЙ байсан (`permission denied for
  schema ...`). Ижил тохиолдол дахин гарвал: `sudo -u postgres psql -d
  etransport_v2 -c "GRANT ..."`.
- **Home directory typo**: `/home/platformabackup/` (нэмэлт "a"-тай), `pwd`-ээр
  ЗААВАЛ баталгаажуулж байгаад зам ашиглах хэрэгтэй.

## ErtHub талын (widget builder) одоогийн бүтэц — товч сануулга

- `content.json` — БҮХ харагдах текст (widgets{}/site{}/ui{}/ui_form{}).
- `metric_registry.json` — виджет → метрикийн холбоос, `agg/filter/transform`
  тайлбар (БОДИТ утга биш, зөвхөн metadata).
- `index.html` доторх `loadMetricRegistry()`/`val()` — метрикийн жинхэнэ утгыг
  тооцоолдог цорын ганц газар.
- **ДҮРЭМ (CLAUDE.md-ээс)**: Тоо ЗОХИОХГҮЙ — эх сурвалж холбогдоогүй бол `0`/`—`.
  Ганц эх сурвалж — динамик утгыг content.json-д бүү бич.

## Энэ chat-д ХИЙГДСЭН ажил (2026-09-06)

Backend (10.2.115.40:3000) хараахан публик URL-гүй тул **бодит холболт хийх
боломжгүй** — тул зөвхөн клиент талын хийцийг (plumbing) урьдчилан бэлдэв,
публик URL гарч ирмэгц нэг мөр тохиргоо солиход идэвхжихээр:

1. `js/backend-config.js` (шинэ) — `ETRANSPORT_BACKEND_BASE` тогтмол
   (`js/firebase-config.js`-тэй ижил зарчим: хоосон бол бүх зүйл disabled).
2. `js/erthub-backend.js` (шинэ) — `EHBackend` модуль (`js/erthub-publish.js`-тэй
   ижил зарчим: enabled/disabled, timeout, алдаа бол `null`, ямар ч тохиолдолд
   сайтыг унагаахгүй). `fetchSectorSummary(sector)` ба
   `fetchRailWagonLoading()` экспортолно.
3. `index.html`: 2 script холбов, `componentDidMount()`-д
   `loadEtransportBackend()` дуудна, `val()`-ийн `getters`-т
   `'rail.wagon_loading':()=>this._railWagonLoading` нэмэв.
4. `metric_registry.json`-ийн `metrics{}`-д `rail.wagon_loading` бүртгэв
   (`quality:"pending"` — код холбогдсон ч АМЬД БИШ гэдгийг ил тэмдэглэсэн).
   Ямар ч widget слот руу ХАРААХАН холбоогүй (аль слотод очихыг Дараагийн
   ажлын жагсаалтын 2-р зүйл шийднэ). Admin-ийн "Виджет бүтээгч"-ийн метрик
   сонголтод одоо харагдана (Object.keys(REG.metrics)-ээс автоматаар), гэхдээ
   `why()` нь "метрик өөрөө verified биш" гэж анхааруулна — энэ бол зөв,
   учир нь бодит холболт хараахан алга.

## Backend URL гарсны дараах чек-лист

Доорх дараалал бүрэн биелэх хүртэл `rail.wagon_loading` нь `quality:"pending"`
хэвээр үлдэнэ — алхам алгасаж яаравчлахгүй, учир нь CLAUDE.md-ийн "Тоо
ЗОХИОХГҮЙ" дүрэм яг ЭНЭ алхмуудаар л хангагдана.

### 0. Сүлжээний тохиргоо — хэн юу хийхээ (2026-09-06 тодруулсан)

- **Cloudflare Tunnel амжилтгүй болсон**: серверийн firewall cloudflared-ийн
  ашигладаг 7844 портыг (TCP ба UDP аль алиныг нь) бүхэлд нь блоклож байна
  (`dial tcp ...:7844: i/o timeout`) — QUIC болон HTTP/2 хоёуланд адилхан.
  ngrok мөн ашиглахгүй гэж шийдсэн (данс үүсгэх шаардлагатай тул).
- **Сонгосон шийдэл**: шинэ домэйн + FortiGate дээр шууд NAT/port-forward.
  - Домэйн: `etransport.data.gov.mn` — ЗӨВХӨН **datacenter**-ээс бүртгүүлнэ
    (хэрэглэгч энэ хэсгийг явуулж байгаа).
  - Public IP: `103.17.108.199` (`.gov.mn`-ий хуваалцдаг IP байж болзошгүй —
    NAT тохируулахдаа тэнд өмнө нь өөр сайт 80/443-аар ажиллаж байгаа
    эсэхийг шалгах хэрэгтэй, байвал raw port-forward биш SNI/hostname-based
    дүрэм хэрэгтэй болно).
  - FortiGate дээрх NAT/firewall (`103.17.108.199:443→10.2.115.40:443`,
    `:80→:80`) **дотоод багаараа ӨӨРСДӨӨ тохируулна** (IT-ийн тусдаа зөвшөөрөл
    ШААРДАХГҮЙ — зөвхөн домэйн бүртгэл datacenter-ээс хамаарна).
  - nginx + Let's Encrypt + CORS (доорх A/B) — DNS ажиллаж, 80/443 нээгдмэгц
    дараагийн chat-д серверийн терминалаар хамт хийнэ.

### A. Backend талд (URL/сервер хандалттай chat-д)
- [ ] `curl -s https://<public-url>/health` → `{"status":"ok"}` шалгах
- [ ] `curl -s https://<public-url>/health/db` → DB холболт шалгах
- [ ] **CORS**: Express backend GitHub Pages-ийн домэйныг (жиш.
      `https://otgonerdene02-cmyk.github.io`) `Access-Control-Allow-Origin`-д
      зөвшөөрсөн эсэхийг шалгах — үгүй бол URL хүрдэг ч хөтчийн fetch CORS
      алдаагаар цуцлагдана (энэ одоогийн кодод шалгагдаагүй асуудал).
- [ ] `curl -s https://<public-url>/api/sectors/rail/summary` — хариуны JSON
      бүтцийг (талбарын нэрс, нэгж) тэмдэглэж авах, доорх C хэсэгт ашиглана.
- [ ] `gold.monthly_summary_by_sector`-ийн rail хэсгийг `silver.rail_wagon_loading`-тай
      холбох эсэхийг (эсвэл тусдаа endpoint үүсгэх эсэхийг) шийдэх — одоогоор
      энэ endpoint `silver.rail_operations`-с уншдаг, шинэ ETL-тэй ХОЛБООГҮЙ.

### B. Клиент тал (widget builder кодод)
- [ ] `js/backend-config.js`-ийн `ETRANSPORT_BACKEND_BASE`-г бөглөх (төгсгөлийн
      "/" ОРУУЛАХГҮЙ)
- [ ] `node scripts/serve.js 8080`-аар локал нээж, DevTools Network-ээр
      `js/erthub-backend.js`-ийн fetch амжилттай хариу авч байгааг (CORS
      алдаагүй) шалгах
- [ ] Console-д `loadEtransportBackend()`-ээс алдаа гарахгүй байгааг шалгах

### C. Метрикийн утга баталгаажуулах (тоо зохиохгүй дүрэм)
- [ ] admin → **"Дата холболт" таб** нээж `rail.wagon_loading` картыг олох
      (ӨМНӨ нь JSON гараар засдаг байсан — ОДОО МАЯГТААР). A хэсгийн бодит
      хариунаас гарсан талбаруудаар `unit`/`filter`/`period_note`
      талбарыг ХУДАЛГҮЙ, БОДИТООР бөглөх (`agg`/`source`/`dataset`/`column`
      нь кодтой холбоотой тул ЗӨВХӨН УНШИХ — эдгээрийг өөрчлөх бол
      `index.html`/`js/erthub-backend.js`-ийг ХАМТ нь өөрчилнө).
- [ ] Утгыг өмнө нь ETL шатанд баталгаажуулсан тоотой (Шивээ-Овоо=322,
      Амгалан=29/89) харьцуулж нийцэж байгааг шалгах
- [ ] Нийцэж байвал "Чанар" dropdown-оор `quality:"pending"` → `"verified"`
      болгох (баталгаажуулах анхааруулга гарна)

### D. Widget builder-т холбох (`admin/index.html`)
- [ ] Аль widget слот руу холбохыг сонгох (жиш. `w2c.sectors.rail` — "Долоо
      хоногийн нийт ачаа тээвэр")
- [ ] Тухайн слотын метрик сонголтоос `rail.wagon_loading`-г сонгох (verified
      болсны дараа disabled биш, идэвхтэй сонголт болно)
- [ ] Preview дээр утга бодитоор гарч ирснийг (0/NaN биш), "Одоогийн холбоос
      дүрэм зөрчиж байна" анхааруулга алга болсныг шалгах
- [ ] Экспортлох → JSON татаж, `metric_registry.json`-г репод шинэчлэх

### E. Тест ба commit (CLAUDE.md-ийн дүрмээр)
- [ ] `node scripts/check-cyrillic.js`
- [ ] `node scripts/check-registry.js`
- [ ] `node tests/run.js` (95/95 pass — H бүлэг "Дата холболт" табыг шалгана)
- [ ] `node tests/text-coverage.js --list` (5/5 pass)
- [ ] Push хийхийн өмнө хэрэглэгчээс зөвшөөрөл авах

## Widget builder — "Дата холболт" таб (шинэ, 2026-09-06)

`admin/index.html`-д 3 дахь таб нэмэгдэв: **"Дата холболт"**
(`S.tab==='metrics'`, `lvMetrics()` функц). `metric_registry.json`-ий
`metrics{}`-ийн МЕТАДАТА (unit/quality/filter/period_note/would_need)-г
ЭНД маягтаар (raw JSON гараар бичихгүйгээр) засварлана — дээрх B/C
алхмуудад JSON бичихийн оронд ЭНЭ табыг ашиглана.

- `source`/`dataset`/`column`/`agg` ЗӨВХӨН УНШИХ (кодтой холбоотой,
  developer-ийн засах ёстой талбар).
- "Чанар" (quality) `mock→verified` шилжихэд "тоо зохиохгүй" дүрмийг
  сануулсан баталгаажуулалт гарна.
- "+ Шинэ метрик (зөвхөн төлөвлөгөө)" нь ЯМАР Ч амьд холболт үүсгэдэггүй
  (`quality:'mock'`-оор эхэлнэ) — зөвхөн "ийм эх сурвалж хэрэгтэй" гэдгийг
  бүртгэнэ.
## Чарт бүтээгч — Tableau маягийн X/Y сонголт (шинэ)

`js/erthub-chart.js` (шинэ дундын модуль) нь **сайт БА admin хоёуланд**
ачаалагдаж, дараах 3 зүйлийг нэг эх сурвалжаас өгнө:

1. **Талбарын каталог** — агаарын feed-ийн 31 талбар (нэр/төрөл/тайлбар/
   утга гаргагч). Өмнө нь `index.html`-ийн `AIR_COLUMNS` байсныг ЭНД
   нүүлгэв (`DS_SCHEMA.air`, Excel татах бүгд урьдын адил ажиллана).
   19 нь **бүлэглэх** (dimension), 11 нь **нэгтгэх** (measure) талбар.
2. **`EHChart.agg(rows,spec)`** — SUM / COUNT / AVG / MIN / MAX нэгтгэл,
   эрэмбэ, топ-N. Сайт ба preview ЯГ ИЖИЛ хөдөлгүүр ашиглана.
3. **`EHChart.validate(spec)`** — буруу тохиргоог `null` болгоно.

**Хэрэглэх зам:** admin → "Дата холболт" таб → доод хэсэг "Чарт бүтээгч".
Чартын төрөл + X тэнхлэг + Y тэнхлэг + нэгтгэл сонгоод preview-г нь
шууд харна. Экспортлох → commit хийхэд сайт дээр гарна.

- Тохиргоо `metric_registry.json → widgets.<id>.chart`-д хадгалагдана.
- **Тохиргоо БАЙХГҮЙ бол сайт өмнөх хатуу чартаа зурна** — иймд
  тохируулаагүй л бол юу ч өөрчлөгдөхгүй (эрсдэлгүй өгөгдмөл).
- Чарт нь **хуудасны Он/Сар/Бүс/Чиглэлийн шүүлтийг ДАГАНА**
  (`_airFilteredRows`) — Tableau-гийн dashboard filter зарчим.
- Гарчиг нь тохиргооноос автоматаар гарна (жиш. "Компани · Зорчигч
  (нийлбэр)") — X тэнхлэгээ сольчихоод хуучин гарчигтай үлдэхгүй.
- Одоогоор идэвхтэй байгаа 2 чарт: `ds_air_monthly`,
  `ds_air_cargo_monthly` (агаарын датасэт хуудас).
### Виджетийн УТГА солих (загвар хэвээр) — гол хэрэгцээ

Виджет засварлах дэлгэцийн дээд талд **"Виджетийн утга — юуг харуулах вэ"**
маягт: **Хэмжигдэхүүн** + **Нэгтгэл**. Чартын ЗАГВАР (байрлал, хэлбэр,
өнгө) ОГТ өөрчлөгдөхгүй — зөвхөн ЯМАР тоог харуулах нь солигдоно.

Жишээ (`ls` виджет): "НИЙТ НИСЛЭГ · 2,450 нислэг" → Хэмжигдэхүүн
"Зорчигч" сонгоход "ЗОРЧИГЧ · 1,849,871 хүн" болно. Хамт өөрчлөгдөх нь:
- **нэгжийн шошго** (нислэг → хүн) — `EHChart.unitOf()`
- **метрикийн нэр** (НИЙТ НИСЛЭГ → ЗОРЧИГЧ)
- **өөрчлөлтийн хувь** — сонгосон хэмжигдэхүүний сар тутмын цуваанаас
  дахин тооцогдоно (хуучин метрикийн хувийг наавал ХУДАЛ мэдээлэл болно)
- **spark line** — өмнө нь `miniSpark()` **зохиомол sin-муруй** байсныг
  сар тутмын **БОДИТ цуваа** болгов ("тоо зохиохгүй" дүрмийн засвар)

Тохиргоо: `metric_registry.json → widgets.<id>.value = {measure, agg}`.
Хоосон бол виджет **өмнөх хэвээрээ**. Одоогоор `ls` виджетэд идэвхтэй
(`VALUE_TARGETS`) — бусад виджетэд нэг мөрөөр өргөтгөнө.

**ЯАГААД ЭНЭ ЗАМ:** слот бүр `FIXED_UNIT`-аар "шаардах нэгж"-тэй тул
метрикийн жагсаалтаас нэгж таарахгүйг (`air.weekly_pax · хүн`) сонгох
БОЛОМЖГҮЙ байв. Тэр хамгаалалт зөв (буруу шошготой тоо гаргахаас
сэргийлдэг) тул устгаагүй — оронд нь нэгжийг УТГАТАЙГАА ХАМТ сольдог
маягт нэмэв.

- **Дараагийн алхам:** нүүр хуудасны t05/i06/r06 — тэдгээр нь
  `buildTrendChart` (MONTHS хатуу) дээр суурилсан тул тусдаа ажил.

- **Одоогоор ХИЙГДЭЭГҮЙ (тусдаа том ажил):** шинэ backend метрикийг
  ЯМАР ЧИГ виджетэд кодгүйгээр амьд холбох. `index.html`-ийн виджет
  бүрийн БОДИТ утгын тооцоолол (`SECTORS`/`WEEK_DATA`/`weeklyAirStats()`
  гэх мэт) тус тусдаа, нэг төвлөрсөн "метрик уншигч" байхгүй тул шинэ
  сектор бүрийг сайтад ЖИНХЭНЭ харуулахад ӨДГӨӨ ч хөгжүүлэгчийн код
  өөрчлөлт (`val()`/тухайн виджетийн функц) ЗААВАЛ шаардлагатай.
  Rail.wagon_loading бүрэн verified болж, ХОЁР ДАХЬ бодит кейс гарсны
  дараа л нэг төвлөрсөн загвар руу шилжих эсэхийг шийдэх (том, тусдаа
  төлөвлөгөө, амьд төрийн порталын regression эрсдэлтэй тул яаравчлахгүй).

## Дараагийн ажлын жагсаалт (бусад, шинэ chat-д)

1. Бусад салбарууд (`road`/`sea`/`transit`) ETL бэлэн болмогц дээрх A–E хэв
   маягийг давтан (config → getter → registry → widget холболт) нэмэгдэнэ.
